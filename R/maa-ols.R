#' Returns the sum of 2 numbers (or numeric vectors)
#'
#' @param a (numeric) : A numeric vector (can contain just a single number)
#' @param b (numeric) : A numeric vector (can contain just a single number)
#'
#' @return
#' @export
my_sum <- function(a, b){
    base::return(a + b)
}
